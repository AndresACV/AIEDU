# Core Web Framework
flask>=2.0.1
werkzeug>=2.0.1

# FastAPI (New Primary Backend)
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6

# SSL/HTTPS Support
pyOpenSSL>=23.0.0            # For SSL certificate generation (used in app.py)

# Audio Processing (Speech-to-Text and Text-to-Speech)
pyaudio>=0.2.11              # Audio input/output
pyttsx3>=2.90                # Text-to-speech engine
SpeechRecognition>=3.8.1     # Speech recognition
vosk>=0.3.45                 # Offline speech recognition models
pydub>=0.25.1                # Audio file manipulation and format conversion
soundfile>=0.10.3            # Audio file I/O
ffmpeg-python>=0.2.0         # FFmpeg Python bindings for audio conversion

# Google Cloud Services (for cloud speech providers)
google-cloud-speech>=2.21.0  # Google Cloud Speech-to-Text
google-cloud-texttospeech>=2.16.3  # Google Cloud Text-to-Speech
google-auth>=2.23.0          # Google Cloud authentication
google-generativeai>=0.8.3   # Google Gemini AI

# RAG System Components
sentence-transformers>=2.2.2 # For embedding generation
chromadb>=0.4.6              # Vector database
requests>=2.28.0             # For Ollama API calls and HTTP requests

# Machine Learning & GPU Support  
torch>=2.0.0                 # PyTorch for ML models and CUDA support
numpy>=1.20.0                # Required by sentence-transformers and audio processing

# Essential Utils
tqdm>=4.62.0                 # Progress bars
typing-extensions>=4.0.0     # Enhanced type hints (used in several modules)
pathlib>=1.0.0               # Path manipulation (though built-in in Python 3.4+)

# System Integration
psutil>=5.8.0                # System and process utilities (optional but recommended)

# Development and Debugging (optional)
# matplotlib>=3.5.0          # For plotting and visualization
# pandas>=1.3.5              # For data analysis

# SYSTEM DEPENDENCIES (must be installed separately):
# 
# For Ubuntu/Debian/WSL2:
# sudo apt update
# sudo apt install -y espeak espeak-data libespeak1 alsa-utils ffmpeg
#
# For Windows:
# - FFmpeg: Download from https://ffmpeg.org/download.html
# - TTS: Windows SAPI5 works automatically (no additional setup)
#
# Test TTS installation:
# Linux/WSL2: espeak "Hello world"
# Windows: TTS works automatically through pyttsx3
